# Install React Query

Run the following command in the `frontend` directory:

```bash
npm install @tanstack/react-query
```

After installing, React Query setup files have been created. You'll need to wrap your app with the QueryClientProvider in `main.tsx`.
